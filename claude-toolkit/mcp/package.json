{
  "name": "claude-toolkit-mcp",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Bundled zero-dependency MCP server for claude-toolkit: verify_result_claim, query_graph, memory_graph_add, placeholder_scan. Runs on plain Node (no npm install).",
  "bin": {
    "claude-toolkit-mcp": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs",
    "smoke": "node smoke-test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}

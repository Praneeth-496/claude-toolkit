# <PROJECT_NAME> — Project Context

## Pointers (read on demand)
- Heavier background (dataset, hardware, calibration, pipeline contract): `.claude/CONTEXT.md`
- Custom skills directory: `~/.claude/skills/`
- Auto-memory index: `~/.claude/projects/<AUTO_MEMORY_DIR>/memory/MEMORY.md`
- Toolkit principles & token-budget rules: `<TOOLKIT_PATH>/docs/principles.md`
- 4-file pattern explanation: `<TOOLKIT_PATH>/docs/4-file-pattern.md`

## Who I am
<ROLE_AND_AFFILIATION>
Focus: <DOMAIN_FOCUS>

## What this is
<ONE_PARAGRAPH_PROJECT_DESCRIPTION>

Current status:
- <MILESTONE_OR_PHASE_1>
- <MILESTONE_OR_PHASE_2>

## Architecture
- <KEY_INPUT_SHAPE_OR_FEATURE_DIM>
- <KEY_COMPONENTS_OR_MODELS>
- <SPLIT_OR_DATASET_SIZE>
- <ARTEFACT_LOCATIONS>  (e.g. saved_models/, results/, experiments/)
- <LIVE_OR_INFERENCE_ENTRYPOINT>  (delete if not applicable)

## HPC / env  (delete this section if not applicable)
- Cluster: <CLUSTER_NAME> (user <CLUSTER_USER>)
- Known partitions: <PARTITIONS>
- **Partition selection rule:** before submitting, ALWAYS check which partitions are free via `sinfo` / `squeue` over SSH and pick any idle partition whose resources match the job. Do not default to a specific partition just because existing scripts use it.
- Suggested check:
  - `sinfo -o "%P %a %l %D %t %C %G"`
  - `squeue -h -o "%P %T" | sort | uniq -c`
- Venv: <VENV_ACTIVATE_PATH>
- Remote host: <HPC_HOST> via ProxyJump <JUMP_HOST>
- SLURM scripts: <SLURM_SCRIPTS_GLOB>
- Logs: <LOG_DIR>
- Scripts are dual-mode: sbatch on HPC, bash locally

## Git
- Main branch is `<MAIN_BRANCH>`
- **NEVER run `git commit` or `git push` without explicit permission.** Both are in the deny list in `settings.local.json` — every invocation prompts the user. Stage changes with `git add`, then stop and ask.
- `.claude/` is gitignored. `CLAUDE.md`, `CONTEXT.md`, and `settings.local.json` are never pushed.

## File conventions
- <SCRIPT_NAMING_PATTERN>  (e.g. training scripts numbered 00_, 01_, 02_…)
- Artefacts: <ARTEFACT_FILE_TYPES>  (e.g. .joblib classical, .pt NN, .npz sequences, .json splits/results)
- Shell scripts: <SHELL_SCRIPT_PATTERN>  (e.g. run_<verb>_<scope>.sh)

## Environment isolation (mandatory)
- **Never install packages globally.** Every project works inside its own isolated env so versions never collide across projects or with the system.
- Python: a project-local `.venv` (created with `uv venv`, or `python -m venv .venv`). Install with `uv pip install ...` or after `source .venv/bin/activate`. Never bare `pip install`, `sudo pip`, or `pip install --user`.
- Node: local `node_modules` only (`npm install`), never `npm install -g`.
- First step in a fresh repo: run the `env-bootstrap` skill. The `block-global-pip` hook will refuse global installs until an isolated env is active.

## Auto-memory caveat
- `~/.claude/projects/<AUTO_MEMORY_DIR>/memory/` may go stale over time
- Trust current code over memory; run the `refresh-memory` skill to re-derive

## Skill auto-routing (proactive, no slash needed)
- Before answering a non-trivial request, scan available skills (the system-reminder lists them) and pick the best match by `description` / TRIGGER phrases. Invoke it directly — do **not** wait for the user to type the `/name`.
- Recall layer: if `~/.claude/projects/<AUTO_MEMORY_DIR>/memory/graph/nodes.jsonl` exists, prefer `query-graph <keyword>` over reading `project_*.md` whole. It returns a 1-hop subgraph in ≤40 lines.
- Durable facts: when the user states a relationship worth keeping ("X depends on Y", "we replaced Z with W", a decision), call `memory-graph add` instead of writing a new `project_*.md`.
- Style/voice skills (`brief`, `godmode`, `10x`, `ghost`, `teacher`, `explainlikeim5`, `critique`, `devil`, `scout`, `compare`, `pitch`, `ooda`) auto-trigger on the phrasings in their descriptions — apply them for that turn, do not announce.
- Rule of thumb: a skill exists, you noticed the trigger, you invoke. Asking the user to confirm the slash is wasted round-trips.

## Code style
- <STYLE_NOTE_1>  (e.g. each pipeline step = one standalone Python file)
- <STYLE_NOTE_2>  (e.g. config in dedicated files like config.py)
- <STYLE_NOTE_3>  (e.g. src/ = reusable lib, training/ = entry points)
- Preserve old variants alongside new (archive/, no_<feature>_ prefix) — do not delete
- <STYLE_NOTE_4>  (e.g. notebooks mirror training script numbering)

## How to respond
- Give code first, explain after only if needed
- No preamble, no "I'll now…" narration
- Short answers, no padding
- Prefer standard idioms for the stack (<LIST_PREFERRED_LIBS>)
- Minimal inline comments unless I ask

## Token saving rules
- Never repeat my question back
- Skip "I'll now..." narration
- Don't summarize what you just did
- Use /compact when context > 80%
- Prefer grep before Read; Edit before Write; subagents for large surveys
- No static `CLAUDE_CODE_MAX_OUTPUT_TOKENS` cap is set — long edits/reviews are allowed to use the full per-response budget. The agent runs across as many responses as the task needs; truncating mid-response just forces re-work.

## Model routing
- Opus 4.7 → intelligent multi-file reasoning (architecture, doc rewrites, pipeline orchestration)
- Sonnet (latest) → small/mechanical skills (verify, rsync, single-file edits)
- Haiku 4.5 → cheap, deterministic skills (path lookups, simple greps). Acceptable for skills with `<5` lines of logic; not for anything that needs judgement.

## <PROJECT_SPECIFIC_SECTION_1>  (delete if unused — e.g. "Thesis", "Deployment", "Dataset")
- <KEY_DOC_PATH>
- <HARD_RULES>  (e.g. no em-dashes, specific citation format)
- <VERIFICATION_RULES>  (e.g. numbers must trace to results JSON)
